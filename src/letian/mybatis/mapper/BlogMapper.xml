<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="letian.mybatis.dao.BlogMapper">

    <resultMap id="findAllResult" type="blog">
        <result property="ownerId" column="owner_id"/>
    </resultMap>

    <resultMap id="blogResult" type="blog">
        <result property="id" column="blog_id"/>
        <result property="ownerId" column="user_id"/>
        <result property="title" column="blog_title"/>
        <result property="content" column="blog_content"/>
        
        <association property="user" javaType="user">
            <id property="id" column="user_id"/>
            <result property="name" column="user_name"/>
            <result property="email" column="user_email"/>
            <result property="password" column="user_password"/>
        </association>
    </resultMap>

    <select id="findById" parameterType="Integer" resultMap="blogResult" resultType="blog">
        select
        blog.id as blog_id,
        blog.title as blog_title,
        blog.content as blog_content,
        user.id as user_id,
        user.name as user_name,
        user.email as user_email,
        user.password as user_password
        from blog, user where blog.id = #{id} and user.id=blog.owner_Id;
    </select>
    
    <select id="findAll" resultMap="findAllResult" resultType="blog">
        select *
        from blog
    </select>

    <select id="findAll2" parameterType="hashMap" resultMap="findAllResult" resultType="blog">
        select *
        from blog limit #{offset}, #{limit}
    </select>

</mapper>